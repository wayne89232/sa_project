<!DOCTYPE HTML>
<script type="text/javascript">
	$(document).ready(function(){
		$('.menu a.item').on('click', function() {
			if(!$(this).hasClass('dropdown')) {
				$(this).addClass('active').closest('.ui.menu').find('.item').not($(this)).removeClass('active');
			}
		});
		
	});
</script>
<!-- Main -->
	<section id="main" class="container">
		　<div ng-hide="is_user">
			<div class="ui secondary pointing four item demo menu">
	  			<a ng-click="show_change(0)" class="active item">Info</a>
	  			<a ng-click="show_change(1)" class="item">Donation</a>
	  			<a ng-click="show_change(2)" class="item">Track List</a>
	  			<!-- <a ng-click="show_change(3)" class="item">Community</a> -->
			</div>
			<div ng-hide="show[0]" class="ui form segment">
				<legend>Personal Information</legend>
				<div class="two fields">
					<div class="field">
						<label>Account</label>
						<input placeholder="Text Input" ng-model="user_info.account" readonly>
					</div>
					<div class="field">
						<label>Password</label>
						<input placeholder="Text Input" type="password" ng-model="user_info.password" readonly>
					</div>
				</div>
				<div class="two required fields">
					<div class="field">
						<label>Name</label>
						<input placeholder="Text Input" type="text" ng-model="user_info.user_name" readonly>
					</div>
					<div class="field">
						<label>Email</label>
						<input placeholder="Text Input" type="text" ng-model="user_info.email" readonly>
					</div>
				</div>
				<div class="two required fields">
					<!-- <div class="field">
						<label>Photo</label>
						<input  placeholder="Text Input" type="text" ng-model="photo_url">
					</div> -->
					<div class="field">
						<label>Birthdate</label>
						<input type="text"  ng-model="user_info.birthdate" readonly/>
					</div>
				</div>
			<!-- 	<div class="required fields">
					<div class="ui green submit button">Update Information</div>
				</div> -->
			</div>
		<div ng-hide="show[1]">
			<table class="ui celled table">
    			<thead>
    				<tr>
    					<th>Event</th>
    					<th>Donated Amount</th>
    					<th>Date</th>
    					<th>Details</th>
    				</tr>
    			</thead>
    			<tbody>
					<tr ng-repeat="donation in donation_list">
						<td ng-bind = "donation.Event.event_name"></td>
						<td ng-bind = "donation.amount"></td>
						<td ng-bind = "donation.date"></td>
						<td><i ng-click="show_event(donation.Event.event_id)" class="browser icon"></i></td>
					</tr>
				</tbody>
   			</table>
		</div>
		<div ng-hide="show[2]">
			<div class="ui cards">
			  	<div class="card" ng-repeat="event in event_list">
			    	<a class="image" ng-click="show_event(event.Event.event_id)">
			      		<img src="/images/J.JPG">
			    	</a>
			   		<div class="content">
			      			<p class="header" ng-bind="event.Event.event_name"></p>
			      		<div class="meta">
			        		<p ng-bind="event.Event.event_date"></p>
			      		</div>
			      		<div class="description">
			      			<p ng-bind="event.Event.description"></p>
			      		</div>
			    	</div>
			  	</div>
			</div>
		</div>
			<div ng-hide="show[3]">
				<table class="ui celled table">
	    			<tbody>
						<tr ng-repeat="friend in friends | filter:search">
							<a><td>
								<div ng-bind = "friend.photo_url"></div>
								<div ng-bind = "friend.user_name"></div>
							</td></a>
						</tr>
					</tbody>
	   			</table>
			</div>
		</div>
		<div  ng-show="is_user">
			<div class="ui secondary pointing one item demo menu">
	  			<a class="active item">Donation</a>
	  		</div>
			<div>
			<table class="ui celled table">
    			<thead>
    				<tr>
    					<th>Event</th>
    					<th>Donated Amount</th>
    					<th>Date</th>
    				</tr>
    			</thead>
    			<tbody>
					<tr ng-repeat="donation in donation_list">
						<td ng-bind = "donation.Event.event_name"></td>
						<td ng-bind = "donation.amount"></td>
						<td ng-bind = "donation.date"></td>
					</tr>
				</tbody>
   			</table>
			</div>
		</div>
	</section>